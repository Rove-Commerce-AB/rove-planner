/** Serializable fallback; must match --color-customer-default / blue/500. */
export const DEFAULT_CUSTOMER_COLOR = "#657dd4";

/** Chart colors – interactive primary, green-600, purple-600, orange-500 */
export const MOCK_CHART_COLORS = ["#657dd4", "#3d764d", "#7b4ce1", "#ef6518"] as const;

/** Must match --color-text-muted / text-tertiary (zinc/400). Used when no project color. */
export const DEFAULT_PROJECT_COLOR = "#a1a3aa";

/** Fallback when calendar hours are missing. Should match typical full-time (e.g. Swedish 40h). */
export const DEFAULT_HOURS_PER_WEEK = 40;
